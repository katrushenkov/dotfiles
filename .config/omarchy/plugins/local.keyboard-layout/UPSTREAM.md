# Keyboard layout

Cloned from `omarchy.keyboard-layout`.

Source: `/usr/share/omarchy/shell/plugins/bar/widgets/KeyboardLayout.qml`

The original remains built into Omarchy. Remove this plugin directory or swap
your bar entry back to `omarchy.keyboard-layout` whenever you want to return to the built-in.
