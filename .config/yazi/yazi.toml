[mgr]
cwd = { fg = "#a7c080" } # Soft Green
show_hidden = true
sort_by = "natural"
sort_dir_first = true

[opener]
extract = []
play = [
	{ run = "mpv %s", orphan = true, for = "unix", desc = "Open with MPV"},
  { run = 'mpv "$@"', orphan = true, for = "unix", desc = "Open with MPV" },
  { run = 'vlc "file://$0"', orphan = true, for = "unix", desc = "Open with VLC" }
]
edit = [
	{ run = "$EDITOR %s", block = true, for = "unix" },
	{ run = "$EDITOR %s", block = true, for = "macos" },
]
open = [
	{ run = "xdg-open %s", desc = "Open" },
]

[open]
#	{ run = "xdg-open %s", desc = "Open" },
rules = [
	{ mime = "text/*", use = "edit" },
	{ mime = "video/*", use = "play" },
	{ mime = "audio/*", use = "play" },

	# { mime = "application/json", use = "edit" },
	{ url = "*.json", use = "edit" },

	# Multiple openers for a single rule
	{ url = "*.html", use = [ "open", "edit" ] },
]
